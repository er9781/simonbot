// check config.

// clone the git repo if it's not already there (check with git status?)
// clone it only with master branch?
// git clone https://username:password@github.com/username/repository.git

// what else?
